apiVersion: v1
kind: Service
metadata:
  name: devops-spring-boot-gitops-dev
  namespace: demo-project
  labels:
    app: devops-spring-boot-gitops
    env: dev
spec:
  selector:
    app: devops-spring-boot-gitops
    env: dev
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 31921
  type: ClusterIP